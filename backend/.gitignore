node_modules
dist
# Keep environment variables out of version control
.env

/generated/prisma

# User-uploaded files — not code, not committed
/uploads

# Seeded test-account credentials (plaintext passwords) — local dev only
test-users.local.md
