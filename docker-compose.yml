services:
  mariadb:
    image: mariadb:11
    container_name: mysentinelcircle-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${MARIADB_DATABASE}
      MARIADB_USER: ${MARIADB_USER}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: mysentinelcircle-backend
    restart: unless-stopped
    env_file:
      - backend/.env
    environment:
      DATABASE_URL: mysql://${MARIADB_USER}:${MARIADB_PASSWORD}@mariadb:3306/${MARIADB_DATABASE}
      FRONTEND_URL: https://localhost:4444
      PORT: "3000"
    volumes:
      - ./backend/src:/app/src
      - ./backend/prisma:/app/prisma
    depends_on:
      mariadb:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: mysentinelcircle-frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: ""
      VITE_GOOGLE_CLIENT_ID: "690453218919-c51tovqmqsabic58n9pk2ockog77b3o9.apps.googleusercontent.com"
      VITE_HMR_CLIENT_PORT: "4444"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    depends_on:
      - backend

  nginx:
    build:
      context: ./nginx
    container_name: mysentinelcircle-nginx
    restart: unless-stopped
    ports:
      - "8081:80"
      - "4444:443"
    volumes:
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - frontend
      - backend

volumes:
  mariadb_data:
