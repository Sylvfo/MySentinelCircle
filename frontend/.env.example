VITE_API_URL="http://localhost:3000"

# Google Cloud Console -> OAuth 2.0 Client (Web application). Same client ID
# as backend/.env's GOOGLE_CLIENT_ID (client IDs are public by design, not
# a secret) — must match for token verification to succeed server-side.
VITE_GOOGLE_CLIENT_ID=""
