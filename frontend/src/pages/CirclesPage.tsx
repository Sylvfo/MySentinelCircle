export function CirclesPage() {
  return (
    <div>
      <h1>Cercles</h1>
      <p style={{ color: 'var(--text-muted)' }}>
        Créez vos cercles (1er cercle, famille, travail...) et gérez qui en fait partie.
      </p>
      <div className="empty-state">Aucun cercle pour l'instant — le module Sentinel n'est pas encore codé.</div>
    </div>
  );
}
